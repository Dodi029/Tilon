# 작업로그

## 2026-05-15

- `codex-request.txt` 요청을 확인했다.
- 현재 프로젝트 구조를 분석했다.
  - 서버 진입점: `app.py`
  - 웹 프레임워크: Flask
  - 기본 실행 명령: `python app.py`
  - 기본 URL: `http://localhost:5000`
- PDF 생성 방식이 `page.pdf()`가 아니라 Playwright 전체 페이지 PNG 스크린샷과 `img2pdf` 변환을 사용하도록 확인했다.
- `app.py` 서버 실행부에 `PORT`, `FLASK_DEBUG` 환경변수 제어를 추가했다.
- `tests/test_single_page_pdf.py`에 이미지 높이와 PDF 페이지 비율 검증을 추가했다.
- `tests/run_server_pdf_flow.py`를 추가해 서버 실행부터 PDF 생성 및 pypdf 검증까지 한 번에 수행하도록 했다.
- `README.md`를 현재 구현 기준의 실행/검증 문서로 정리했다.
- `docs/테스트결과.md`를 추가해 검증 결과를 기록했다.
- 추가 요청에 따라 pypdf의 1페이지 검증만으로 성공 처리하지 않도록 보강했다.
- DOM scrollHeight, body scrollHeight, content wrapper scrollHeight, max element bottom을 측정하도록 변경했다.
- `full_page=True` 의존을 제거하고 screenshot `clip`에 전체 콘텐츠 높이를 명시하도록 변경했다.
- PNG 높이, PDF 페이지 크기, PNG/PDF 비율 검증을 추가했다.
- `output/debug_fullpage.png`를 저장해 실제 캡처 결과를 확인할 수 있게 했다.

## 구현 메모

- 긴 PDF 생성은 `app.py`의 `save_screenshot_as_single_page_pdf()`에서 수행한다.
- 전체 문서 높이와 주요 wrapper 높이를 측정한 뒤 `page.screenshot(..., clip={...})`로 PNG를 만든다.
- `img2pdf.get_layout_fun((image_width, image_height))`로 이미지 비율 그대로 PDF 1페이지를 만든다.
- 생성 후 `assert_single_page_pdf()`가 `pypdf.PdfReader`로 페이지 수를 검증한다.
- 생성 후 PDF 페이지 크기 비율이 PNG 비율과 맞는지 추가 검증한다.

## 2026-05-15 - backup working single-page pdf version

- 현재 상태를 기능상 사용 가능한 백업 버전으로 기록했다.
- 백업 기준 기능:
  - Flask 로컬 서버 실행: `python app.py`
  - 접속 URL: `http://localhost:5000`
  - HTML/URL 입력을 Playwright로 렌더링
  - DOM/body/wrapper/max element bottom 높이 측정
  - screenshot `clip` 기반 전체 높이 PNG 생성
  - `img2pdf` 기반 1페이지 PDF 변환
  - `pypdf` 기반 페이지 수 및 PDF 크기 검증
  - debug PNG 저장: `output/debug_fullpage.png`
- 이 기록은 Git 커밋 및 GitHub push 전 백업 기준점이다.

## 2026-05-15 - crop and margin correction

- 실제 콘텐츠 bounding box 기반으로 캡처 후 PNG를 보정하도록 변경했다.
- 원본 screenshot 크기와 crop 후 이미지 크기를 결과에 포함했다.
- 하단 빈 공간 제거량, 좌측 여백, 우측 여백을 결과에 포함했다.
- PDF 폭은 기존 A4 기준 `794px`를 유지하고, 콘텐츠를 중앙 정렬해 좌우 여백이 같도록 했다.
- 기존 `page.pdf()` 미사용, 긴 1페이지 PDF 구조는 유지했다.

## 2026-05-15 - height tolerance correction

- PNG height가 expected height보다 약간 작아도 즉시 실패 처리하지 않도록 변경했다.
- 허용 오차는 `max(150px, expected_height의 1%)`로 계산한다.
- height difference가 허용 오차를 초과하는 경우에만 변환 실패로 처리한다.
- 기존 긴 1페이지 PDF 생성 구조, `page.pdf()` 미사용, crop/padding 보정 구조는 유지했다.

## 2026-05-15 - final image based pdf sizing

- PDF page height가 expected/wrapper/scrollHeight가 아니라 crop 완료 후 final image size 기준으로 결정되도록 확인했다.
- PNG 픽셀 분석으로 content bounding box를 계산해 하단 빈 배경 영역을 제거하도록 보정했다.
- 좌우 여백 before/after를 기록하고, 최종 이미지 기준 좌우 여백이 같도록 유지했다.
- 최종 PDF 폭은 기존대로 794px이며, PDF는 계속 1페이지로 생성된다.
- `page.pdf()`는 사용하지 않았다.

## 2026-05-15 - backup 95 percent complete single-page pdf version

- 현재 상태를 95% 완료된 사용 가능 백업 버전으로 기록했다.
- 백업 기준 기능:
  - 긴 1페이지 PDF 생성 성공
  - `page.pdf()` 미사용
  - screenshot `clip` 기반 PNG 생성
  - 하단 crop 유지
  - 최종 PNG 기준 PDF page size 생성
  - PDF 폭 794px 유지
  - pypdf 1페이지 검증 통과
- 남은 작업: PDF 뷰어/PNG/PDF 배치 관점의 좌우 여백 대칭 문제 분석 및 보정.

## 2026-05-15 - left/right margin correction after backup

- 95% 완료 백업 커밋 이후 좌우 여백 문제만 수정했다.
- final PNG를 PDF 생성 직전에 다시 픽셀 분석하고, 좌우 여백 차이가 5px 이상이면 자동 재중앙 정렬하도록 보강했다.
- final PNG width와 PDF page width가 모두 794px이므로 PDF image placement는 `x=0`으로 기록했다.
- 하단 crop, 1페이지 PDF, `page.pdf()` 미사용 구조는 유지했다.
- 이 2단계 수정은 git commit/push 하지 않았다.

## 2026-05-15 - v1.0 usable release backup

- 현재 상태를 v1.0 사용 가능 버전으로 기록했다.
- v1.0 기준 기능:
  - 긴 1페이지 PDF 생성
  - `page.pdf()` 미사용
  - screenshot `clip` 기반 PNG 생성
  - 하단 crop 유지
  - 좌우 여백 보정 유지
  - 최종 PNG 기준 PDF page size 생성
  - PDF 폭 794px 유지
  - pypdf 1페이지 검증 통과
- 이 기록은 `release v1.0 working single-page pdf converter` 커밋 및 `v1.0` 태그 기준점이다.

## 2026-05-15 - v1.1 image quality/background improvement

- v1.0 태그 이후 v1.1 개선 작업을 시작했다.
- PNG/PDF 배경 경계가 보이지 않도록 최종 PNG를 white background로 flatten 처리한다.
- screenshot `device_scale_factor`를 scale 옵션으로 분리했다.
- scale 2를 사용해 고해상도 PNG를 만들되, PDF page width는 계속 794px로 유지한다.
- 하단 crop, 좌우 여백 보정, 1페이지 PDF, `page.pdf()` 미사용 구조는 유지했다.
- 고해상도 PNG 픽셀 분석 성능을 위해 Pillow `ImageChops.difference(...).getbbox()` 기반 bounding box 계산으로 최적화했다.
- Flask 서버는 변환 중 상태 조회가 막히지 않도록 threaded mode로 실행한다.

## 2026-05-15 - v2.0 stable backup

- 현재 상태를 v2.0 안정 버전으로 기록했다.
- v2.0 기준 기능:
  - Notion HTML/URL을 긴 1페이지 PDF로 변환
  - `page.pdf()` 미사용
  - Playwright screenshot `clip` 기반 PNG 생성
  - final PNG white background flatten
  - 하단 crop 유지
  - 좌우 여백 보정 유지
  - scale 1/2/3 구조 지원
  - 기본 scale 2 고화질 캡처
  - PDF page width 794px 유지
  - pypdf 1페이지 검증 통과
- 다른 PC 또는 새 Codex 세션에서 이어받을 수 있도록 다음 문서를 정리했다.
  - `docs/현재상태.md`
  - `docs/개발환경구조.md`
  - `docs/프로젝트구조.md`
  - `docs/세션복원가이드.md`

## 2026-05-16 - quality UI and OCR pipeline

- `codex-request.txt` 기준 현재 상태를 재확인했다.
- UI에 누락되어 있던 `qualityScale` 선택 항목을 추가했다.
  - 기본값은 scale 2 고화질이다.
  - scale 1/2/3을 UI에서 직접 선택할 수 있다.
- OCR 선택 항목을 추가했다.
  - 기본값은 자동이다.
  - 사용자가 끄면 변환 요청에 `ocr=false`가 전달된다.
- `ocrmypdf`가 설치된 환경에서는 PNG 기반 PDF 생성 후 OCR 텍스트 레이어를 적용하도록 추가했다.
  - 기본 OCR 언어는 `kor+eng`이다.
  - `OCR_LANG` 환경변수로 변경할 수 있다.
  - `ocrmypdf`가 없거나 실패해도 기존 이미지 기반 1페이지 PDF 생성은 유지하고 상태를 결과에 남긴다.
- 테스트가 로컬 OCR 설치 여부에 흔들리지 않도록 테스트 요청에서는 OCR을 명시적으로 끄도록 정리했다.
- `page.pdf()`는 다시 도입하지 않았다.
- git commit/push는 하지 않았다.

## 2026-05-16 - OCR comparison after install

- Mac 환경에서 `ocrmypdf`와 `tesseract` 설치 상태를 확인했다.
  - `ocrmypdf`: `/opt/homebrew/bin/ocrmypdf`
  - `tesseract`: `/opt/homebrew/bin/tesseract`
  - `kor`, `eng` 언어 데이터 확인 완료
- OCR 적용 PDF와 비적용 PDF를 같은 HTML 샘플에서 각각 생성하는 비교 스크립트를 추가했다.
  - `tests/run_ocr_comparison.py`
  - 비적용 PDF: `output/ocr_comparison_no_ocr.pdf`
  - 적용 PDF: `output/ocr_comparison_with_ocr.pdf`
- 검증 대상은 Windows 경로, PowerShell 명령어, 코드블록이다.
- OCR 비적용 PDF는 pypdf 텍스트 추출 결과 0자로, 텍스트 선택/복사가 불가능한 이미지 PDF 상태를 확인했다.
- OCR 적용 PDF는 `ocr_status=applied`이며 텍스트 추출 결과가 생성되어 선택/복사 가능한 텍스트 레이어가 생겼다.
- 기본 `kor+eng` OCR에서 Windows 경로와 일부 명령/코드 라인은 정확히 복사 가능했다.
- `$env:PORT=5055`의 `0`이 `@`로 인식되거나, `python -m ... -s ...`의 `s`가 `5`로 인식되는 등 일부 코드/명령 특수문자 정확도 한계가 확인됐다.
- `OCR_LANG=eng`, `--oversample 300`, `--tesseract-pagesegmode 6` 실험도 수행했으나 항목별로 정확도 개선과 부작용이 섞여 기본 옵션을 유지했다.
- git commit/push는 하지 않았다.

## 2026-05-16 - Cloudflare Tunnel deployment guide

- 현재 Mac 개발 서버를 Cloudflare Tunnel로 외부 공개하는 절차를 문서화했다.
- `docs/배포가이드.md`를 새로 추가했다.
- `README.md`에 Cloudflare Tunnel 요약을 추가했다.
- `docs/현재상태.md`에 현재 공개 구조와 주의사항을 기록했다.
- `docs/세션복원가이드.md`에 Cloudflare Tunnel 실행 방법을 추가했다.
- 기능 코드는 수정하지 않았다.

## 2026-05-16 - dynamic chunk OCR improvement

- OCR 정확도와 안정성을 우선하도록 OCR 구조를 개선했다.
- 기존 `ocrmypdf` 단일 후처리 대신 최종 PNG 기반 chunk OCR 구조로 변경했다.
- 최종 PDF는 계속 1페이지로 유지한다.
- 최종 고해상도 PNG를 높이 기준으로 여러 chunk로 나눈다.
- chunk별로 grayscale, contrast, sharpen, threshold/binarize 전처리 후보를 적용한다.
- Tesseract TSV 결과 중 추출 텍스트 길이가 가장 좋은 후보를 chunk별로 선택한다.
- OCR 좌표를 최종 1페이지 PDF 좌표로 변환해 pypdf로 invisible text layer를 추가한다.
- OCR 적용 후 추출 가능한 텍스트 길이를 검사한다.
- OCR 결과가 부족하거나 실패해도 이미지 기반 PDF 생성 자체는 실패하지 않도록 유지했다.
- `tests/run_ocr_comparison.py`에 제목, 본문, 리스트, Windows 경로, PowerShell 명령어, 코드블록 검증과 chunk 로그 출력을 추가했다.
- `docs/테스트결과.md`와 `docs/현재상태.md`를 최신화했다.
- `page.pdf()`는 다시 도입하지 않았다.
- git commit/push는 하지 않았다.

## 2026-05-16 - DOM text layer mode

- OCR만으로 텍스트 레이어를 만들지 않고 DOM 원본 텍스트 기반 레이어를 추가했다.
- 텍스트 레이어 모드를 `none`, `ocr`, `dom`, `hybrid`로 분리했다.
- Playwright 렌더링 후 DOM text node의 `textContent`, 위치, font size, font weight, line height를 수집한다.
- screenshot scale, crop, 중앙 정렬 보정 값을 반영해 DOM 좌표를 최종 1페이지 PDF 좌표로 매핑한다.
- DOM 텍스트 레이어는 pypdf invisible text layer로 삽입한다.
- hybrid 모드에서는 DOM 텍스트를 먼저 사용하고, OCR은 DOM 텍스트 박스와 겹치지 않는 영역에 보조로 사용한다.
- `tests/run_ocr_comparison.py`를 OCR/DOM/Hybrid 비교 검증으로 확장했다.
- `MasterVD`, `MasterVD2`, `HOST01`, `C:\ClusterStorage`, `프로비전가능`, PowerShell 명령어, 코드블록 추출 정확도를 비교했다.
- DOM 기반 결과는 `output/dom_text_layer_result.txt`에 저장한다.
- `docs/테스트결과.md`와 `docs/현재상태.md`를 최신화했다.
- 기존 긴 1페이지 PDF, 하단 crop, 좌우 여백 보정, 고화질 캡처 구조는 유지했다.
- `page.pdf()`는 다시 도입하지 않았다.
- git commit/push는 하지 않았다.

## 2026-05-16 - v3.0 OCR 개선 백업 버전

- 현재 상태를 정식 v3.0 OCR 개선 백업 버전으로 기록한다.
- v3.0 기준 기능:
  - Notion HTML/URL을 긴 1페이지 PDF로 변환
  - `page.pdf()` 미사용
  - Playwright screenshot `clip` 기반 PNG 생성
  - final PNG white background flatten
  - 하단 crop 유지
  - 좌우 여백 보정 유지
  - scale 1/2/3 고화질 캡처 지원
  - 기본 scale 2 고화질 캡처
  - PDF page width 794px 유지
  - pypdf 1페이지 검증 유지
  - 텍스트 레이어 모드 `none`, `ocr`, `dom`, `hybrid` 지원
  - DOM 원본 텍스트 기반 invisible text layer 지원
  - OCR chunk 기반 보조 텍스트 레이어 지원
  - hybrid 모드에서 DOM 텍스트 우선, OCR 보조 적용
- v3.0 검증 기준:
  - OCR 비적용 PDF 추출 텍스트 0자
  - OCR 기반 PDF 추출 텍스트 1715자
  - DOM 기반 PDF 추출 텍스트 1677자
  - Hybrid PDF 추출 텍스트 3020자
  - 모든 비교 PDF 1페이지 유지
  - `MasterVD`, `MasterVD2`, `HOST01`, `C:\ClusterStorage`, `프로비전가능`, PowerShell 명령어, 코드블록 DOM 기반 정확 추출 확인
  - 단위 테스트 통과
  - 서버 플로우 테스트 통과
- 이 기록은 `release v3.0 improved ocr single-page pdf converter` 커밋 및 `v3.0` 태그 기준점이다.

## 2026-05-17 - v3.0 기준 최소 개선

- v3.0 안정 기능을 기준으로 최소 개선만 적용했다.
- Notion 공개 페이지 상단 로그인/가입 배너를 캡처 전에 숨기도록 했다.
- 제거 대상 문구는 `You're almost there`, `sign up to start building in Notion today`, `Sign up or login`이다.
- 제거 후보는 `role=banner`, `header`, `nav`, fixed/sticky top 요소로 제한했다.
- 실제 문서 본문 컨테이너는 제거하지 않도록 보호했다.
- DOM 텍스트 레이어는 기존 `TreeWalker`와 `Range.getClientRects()` 기반을 유지했다.
- Hybrid 모드에서 DOM 텍스트 y-band와 겹치는 OCR 텍스트를 제외해 중복 삽입을 줄였다.
- `tests/run_banner_text_validation.py`를 추가해 배너 제거와 필수 문장 추출을 검증했다.
- `.gitattributes`를 추가해 Markdown/TXT UTF-8 정책을 기록했다.
- `docs/AI작업인수인계.md`, `docs/Gemini_CLI_인수인계.md`를 추가했다.
- `python -m unittest discover -s tests`, `python tests/run_server_pdf_flow.py`, `python tests/run_ocr_comparison.py`, `python tests/run_banner_text_validation.py`를 통과했다.
- git commit/push는 하지 않았다.

## 2026-05-17 - 상시 운영 구조와 DB/cleanup 준비

- v3.0 안정 PDF 생성 구조를 유지한 상태에서 운영 보조 기능을 추가했다.
- SQLite DB 모듈 `db.py`를 추가하고 DB 파일 위치를 `instance/notion_pdf.db`로 정했다.
- URL 변환과 HTML 업로드 변환의 성공/실패 내역을 DB에 기록하도록 했다.
- DB 기록 실패가 PDF 변환을 중단하지 않도록 `safe_record_conversion`으로 보호했다.
- PDF 생성 후 추출 텍스트를 TXT 파일로 저장하고 경로를 DB에 기록하도록 했다.
- 관리자 확인용 `/admin/conversions` 페이지를 추가했다.
- 오래된 DB 기록과 output 파일을 삭제하는 `cleanup_old_records.py`를 추가했다.
- Mac launchd용 Flask, Cloudflare quick tunnel, cleanup plist 예시를 추가했다.
- `docs/배포가이드.md`, `docs/현재상태.md`, `README.md`, `docs/AI작업인수인계.md`를 상시 운영 기준으로 갱신했다.
- git commit/push는 하지 않았다.

## 2026-05-17 - v3.2 상시 운영/DB/cleanup 구조 추가

- 현재 상태를 정식 `v3.2` 백업 버전으로 기록한다.
- 긴 1페이지 PDF 생성 안정 버전을 유지한다.
- `page.pdf()`는 사용하지 않는다.
- `full_page=True` 의존은 제거된 상태를 유지한다.
- Cloudflare Tunnel 외부 공개 구조를 문서화했다.
- SQLite DB 저장 기능을 추가했다.
- 관리자 변환 내역 페이지 `/admin/conversions`를 추가했다.
- 7일 보관 기준 자동 cleanup 기능을 추가했다.
- Mac launchd 자동 실행 예시를 추가했다.
- UTF-8 문서 인코딩 정책을 적용했다.
- Gemini CLI 인수인계 문서를 추가했다.
- 이 기록은 `release v3.2 persistent server database cleanup and deployment support` 커밋 및 `v3.2` 태그 기준점이다.

## 2026-05-18 - v3.3 업로드 원본 저장 및 관리자 다운로드 기능 백업

- 현재 상태를 정식 `v3.3` 백업 버전으로 기록한다.
- 사용자 업로드 원본 파일을 `uploads/` 폴더에 저장하도록 했다.
- 변환 결과 PDF, PNG, TXT 파일을 `output/` 폴더에 저장하도록 했다.
- SQLite DB에 원본/결과 파일 경로를 기록하도록 했다.
- DB 컬럼 `input_file_path`, `input_file_size`, `input_original_name`, `output_png_path`를 추가했다.
- 관리자 페이지 `/admin/conversions`에서 변환 내역을 확인할 수 있다.
- 관리자 페이지에서 원본/PDF/TXT/PNG 다운로드 링크를 제공한다.
- 다운로드 라우트는 DB id와 파일 종류를 기준으로 동작하며, `uploads/`와 `output/` 하위 파일만 허용한다.
- cleanup에서 만료된 DB 기록과 원본/결과 파일을 함께 삭제하도록 했다.
- 긴 1페이지 PDF 생성 기능은 유지했다.
- `page.pdf()` 미사용과 `full_page=True` 의존 제거 상태를 유지했다.
- 이 기록은 `release v3.3 upload storage admin downloads and cleanup` 커밋 및 `v3.3` 태그 기준점이다.

## 2026-05-19 - v3.4 output 구조 정리 및 파일명 개선

- output 폴더를 `pdf`, `png`, `txt`, `debug`, `tests` 하위 구조로 정리했다.
- uploads 폴더는 `YYYY-MM` 월별 하위 폴더 구조로 정리했다.
- 변환 결과 파일명은 `YYYYMMDD_HHMMSS_원본이름_작업ID.ext` 규칙으로 통일했다.
- DB의 `input_file_path`, `output_pdf_path`, `output_txt_path`, `output_png_path`가 새 구조를 저장하도록 했다.
- 관리자 다운로드는 새 경로에서도 정상 동작한다.
- cleanup은 `uploads/`와 `output/` 하위 폴더를 재귀적으로 확인한다.
- 긴 1페이지 PDF 생성 기능을 유지했다.
- `page.pdf()` 미사용과 `full_page=True` 의존 제거 상태를 유지했다.
- 이 기록은 `release v3.4 output structure cleanup and naming improvements` 커밋 및 `v3.4` 태그 기준점이다.

## 2026-05-20 - v3.5 Notion 토글 자동 펼치기 및 배너 제거

- 캡처 전 Notion 토글 자동 펼치기 기능을 추가했다.
- `details` 구조와 `aria-expanded="false"`인 Notion toggle 후보를 처리한다.
- 중첩 토글을 고려해 여러 pass로 반복 처리한다.
- 토글 펼치기 후 DOM 높이를 다시 측정해 캡처 높이에 반영한다.
- Notion 상단 로그인/가입 배너 제거 옵션을 UI에 추가했다.
- 토글 자동 펼치기와 배너 제거 옵션은 기본 켜짐이다.
- 전처리 실패 시 PDF 생성은 계속되도록 로그만 남긴다.
- 샘플 HTML 테스트에서 숨겨진 토글 내부 텍스트 추출과 배너 문구 제거를 검증했다.
- 긴 1페이지 PDF 생성, output/uploads 저장, DB 기록, 관리자 다운로드, cleanup 구조를 유지했다.
- 이 기록은 `release v3.5 notion toggle expansion and banner cleanup` 커밋 및 `v3.5` 태그 기준점이다.

## 2026-05-21 - v3.6 긴 페이지 chunk 렌더링 및 clipping 완화

- 긴 페이지 단일 screenshot을 내부 chunk 캡처 후 stitch 구조로 변경했다.
- 기본 chunk 높이는 `8000px`이며, 실패 chunk는 retry한다.
- `page.pdf()`와 `full_page=True`는 계속 사용하지 않는다.
- 캡처 전 폰트와 이미지 로딩을 기다리고 lazy-load 이미지를 위해 스크롤한다.
- table/database 주변 overflow, max-height, clip-path, contain clipping 완화 스타일을 적용한다.
- clipping 후보 bounding box를 `output/debug/*_clipping_blocks.json`에 기록한다.
- chunk PNG는 `output/debug/chunks_*`에 저장된다.
- 최종 PDF는 계속 1페이지로 생성된다.
- 긴 페이지, table 이미지, 토글/배너, 서버 플로우, cleanup 테스트를 통과했다.
- 이 기록은 `release v3.6 long page chunk rendering and clipping fixes` 커밋 및 `v3.6` 태그 기준점이다.
